services:
  - type: web
    name: slic-index
    runtime: static
    buildCommand: npm install && tsc -b && vite build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
